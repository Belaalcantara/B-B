import { View } from "react-native";
import styles from "./styles";

const avaliation = ({ data }) => {
    return(
        <View style={styles.container}>

        </View>
    );
}

export default avaliation;